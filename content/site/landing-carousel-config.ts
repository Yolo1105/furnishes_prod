/** Time each slide stays fully readable before crossfading to the next */
export const LANDING_ROTATE_MS = 5000;
/** Crossfade duration between slides (keep < LANDING_ROTATE_MS for calm pacing) */
export const LANDING_FADE_MS = 1600;
