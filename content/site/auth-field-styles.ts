import type { CSSProperties } from "react";

/** Text inputs, Google row, and soft cards on marketing auth pages */
export const authFieldControl: CSSProperties = {
  background: "var(--auth-field-fill)",
  borderColor: "var(--auth-field-border)",
  color: "var(--foreground)",
};

export const authCardSoft: CSSProperties = {
  background: "var(--auth-field-fill)",
  borderColor: "var(--auth-field-border)",
};
