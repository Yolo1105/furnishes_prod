import { AccountSegmentLoading } from "@/components/eva-dashboard/account/account-segment-loading";

export default function PlaybooksLoading() {
  return <AccountSegmentLoading eyebrow="PLAYBOOKS" cards={4} columns={2} />;
}
