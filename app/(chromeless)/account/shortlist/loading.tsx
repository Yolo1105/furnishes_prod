import { AccountSegmentLoading } from "@/components/eva-dashboard/account/account-segment-loading";

export default function ShortlistLoading() {
  return <AccountSegmentLoading eyebrow="SHORTLIST" cards={4} columns={2} />;
}
