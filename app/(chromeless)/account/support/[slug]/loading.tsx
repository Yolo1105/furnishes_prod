import { AccountSegmentLoading } from "@/components/eva-dashboard/account/account-segment-loading";

export default function SupportArticleLoading() {
  return <AccountSegmentLoading eyebrow="SUPPORT" variant="minimal" />;
}
