import { AccountSegmentLoading } from "@/components/eva-dashboard/account/account-segment-loading";

export default function ProfileTabLoading() {
  return <AccountSegmentLoading eyebrow="PROFILE" cards={3} columns={1} />;
}
