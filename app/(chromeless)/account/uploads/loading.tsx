import { AccountSegmentLoading } from "@/components/eva-dashboard/account/account-segment-loading";

export default function UploadsLoading() {
  return <AccountSegmentLoading eyebrow="UPLOADS" cards={4} columns={2} />;
}
