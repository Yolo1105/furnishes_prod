"use client";

import { Toaster } from "@/components/eva-dashboard/ui/sonner";

/** Same as `chatbot_v3` — themed Sonner using CSS variables from `.eva-dashboard-root`. */
export function EvaToaster() {
  return <Toaster />;
}
