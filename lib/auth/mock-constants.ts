/**
 * Canonical mock user id for demo / mock-auth flows.
 *
 * Used by `resolveSession` fallbacks, support ticket acting user, and
 * in-memory store display labels. Change in one place only.
 */
export const MOCK_USER_ID = "mock-user-id" as const;
