/** DPO contact for account Studio surfaces (align with legal pages when updating). */
export const ACCOUNT_DPO_EMAIL = "dpo@furnishes.sg" as const;
export const ACCOUNT_DPO_MAILTO = `mailto:${ACCOUNT_DPO_EMAIL}` as const;
